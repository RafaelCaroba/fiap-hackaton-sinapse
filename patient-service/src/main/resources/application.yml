server:
  port: 8081

spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/sinapse
    username: postgres
    password: postgres

  jpa:

    hibernate:
      ddl-auto: update

    properties:

      hibernate:
        default_schema: patient
        format_sql: true



    show-sql: false

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest